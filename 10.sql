SELECT name FROM people JOIN directors ON people.id = directors.person_id JOIN ratings ON directors.movie_id = ratings.movie_id WHERE rating >= 9.0 AND people.id = directors.person_id GROUP BY person_id;